# 织灵项目交付约定

- 用户要求：从 V2.1.0 起，每次功能修改完成后都生成新版 Windows 安装包，复制到 D 盘根目录，并同步代码到 `Symplatt/zhiling` 公开 GitHub 仓库、发布对应 Release。除非用户明确改变此要求，不需重复询问。
- 用户再次明确：更新完成后直接将最新版安装包交付到本机 D 盘，无需用户自行前往 GitHub 下载；最终回复提供本机文件链接。已经生成并核验的同一安装包可直接复制，无需重复下载。
- 从 2.1.4 起，不再为每次更新新建或发布 TXT 说明；安装使用说明维护在 README，已有历史 TXT 保留。源码打包教程更新当前版本。Release 附带安装包及 SHA-256 校验文件。
- 保留浏览器版和 Electron 桌面版；不要擅自切换用户已有快捷方式。
- 作品数据只保存在本机。禁止把 `data`、用户书架、用户头像、测试产物、运行缓存提交到 GitHub或打包；只使用合成数据进行验证。
- 保存角色位置仅用于本机恢复，导入/导出的 JSON 不包含本机位置。PNG 保留当前布局。
- 打包前完成必要测试与构建；用独立测试书架验证最终包。交付后核对 D 盘安装包和 Release 附件的校验值，并如实报告受阻或未完成的步骤。

## 版本记录与代码维护

- 从 2.1.1 起，每次发布维护 `CHANGELOG.md`，遵循 Keep a Changelog 的版本、日期和分类结构；不补写之前版本。
- 提交采用 Conventional Commits：`type(scope): summary`。版本提交正文必须有 `Added / 新增`、`Changed / 更新`、`Removed / 删除`、`Fixed / 修复`、`Validation / 验证`；无变更的分类明确写“无”。说明用户可感知行为和相关缺陷，不用空泛的“优化”。
- 新代码注释解释设计原因、状态约束、平台差异及错误处理；公共复杂函数用简明文档注释。不要机械重复代码，也不要写未经验证的保证。
- 版本号、CHANGELOG、Release、安装包和使用说明保持一致。验证尚未通过时不得标注已通过。
