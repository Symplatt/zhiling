# 织灵 2.1.6 验证记录

验证日期：2026-09-23，Windows x64，使用独立合成书架。

- 14 个测试文件、43 项测试通过；TypeScript、Vite、Windows NSIS 与便携版构建通过。
- 自动测试覆盖 3、4、5、9、25、100 个节点的等半径与等边长；真实浏览器中 8 人图谱依次调整五档头像和稀疏程度，半径与边长误差均小于 1e-6。新增第 9 个角色后仍为正九边形，截图目视检查通过。
- 关闭头像姓名后，已有头像的角色标签为空，无头像角色姓名保留；刷新后选项保持。自动测试验证 PNG 使用的独立快照同样隐藏姓名，调整大小不会重置设置。
- 预置 2020 年编辑时间和手动坐标，打开、切换姓名开关、刷新后时间不变；自动布局或新增角色后时间更新，重复保存不更新。
- 五个主题逐一核对布局选择框的焦点外框与边框，均与主题强调色一致。
- 最终 Electron 包通过 PNG 保留手动布局和头像、独立导出目录、同名自动编号、取消导出、原生全屏及 120 张关系网落盘回归。记录：`test-results/v216-desktop-final/result.json`。
- 归档版本、Electron 入口、预加载脚本及前端资源与当前源码匹配，无私人数据和测试目录。
- D 盘安装包与发布附件均为 123749482 字节，SHA-256：`efd01899f67d5ce36ed04803930a88b23785576c6069d8716febe3fd0ef2db6b`。没有新建 TXT 使用说明。

## 2.1.5 历史验证记录

验证日期：2026-09-23，Windows x64，使用独立合成书架。

- 13 个测试文件、40 项测试、TypeScript、Vite 及 Windows 安装包构建通过。
- 真实浏览器设为黄金夜、稀疏 2 档、头像 1 档，连续关闭和打开设置三次：焦点均位于关闭按钮，稀疏 1 档未选中且外框样式为 none，已选档位不变，滚动位置保持顶部。
- Tab 可以到达已选中的单选项；方向键切换到 3 档，显示 2px 键盘焦点外框。
- 最终桌面包验证原生全屏、PNG 保持手动布局与头像、独立导出目录、同名自动编号、取消导出和 120 张关系网落盘通过；记录：`test-results/v215-desktop-final/result.json`。
- 打包归档与当前入口、预加载及前端资源匹配，无私人数据和测试目录。
- D 盘安装包与发布附件均为 123749128 字节，SHA-256：`7b80e345e4161761d98ba78a3d106ffaa974b7b2c8d12b2d7aa65d7eac5841b4`。

## 2.1.4 历史验证记录

验证日期：2026-09-23，Windows x64，使用独立合成书架。

- 13 个测试文件、40 项测试通过；TypeScript、生产构建、Windows NSIS 与便携版构建通过。
- 自动测试覆盖两套独立档位基准：头像 3 档为 1.0 倍、稀疏 3 档为 1.8 倍，并验证全部五档及来回切换无累计漂移。
- 浏览器逐档点击实测头像直径为 45.6、60.8、76、91.2、106.4，关系字号为 9、12、15、18、21，线宽同比例改变。刷新后档位保持。
- 黄金夜设置窗口实测宽 580px、左右内边距 36px、内容滚动区内边距 18px；滚动条采用 #44413b，悬停 #625b4c。截图目视检查通过。
- 980×620 窗口内滚动前后“完成”按钮坐标不变，按钮底部位于可视范围。
- 最终桌面包通过原生全屏、PNG 保留坐标与头像、独立 PNG/JSON 导出位置、同名自动编号、取消导出及 120 张关系网落盘回归。记录：`test-results/v214-desktop-final/result.json`。
- 安装包内版本与前端资源、Electron 入口和预加载脚本匹配，不含私人作品和测试目录。
- D 盘安装包 123749133 字节，与发布附件 SHA-256 一致：`4676f106ec1378f0f7a281b78f18f5b06811a4f304f74ebc19fb948be3904d60`。本次不生成 TXT 使用说明。

## 2.1.3 历史验证记录

验证日期：2026-09-22，Windows x64，全部使用独立合成书架。

- 13 个测试文件、39 项测试通过；TypeScript、Vite 生产构建和 Windows NSIS 安装包构建通过。
- 新增 8 节点环形布局回归：800 和 2400 像素边界下，相邻节点距离一致，节点不重叠，最大头像直径占整体最长边超过 17%。
- 最终桌面包的 8 人合成图谱在密度 1、头像 5 时，头像直径占整体最长边约为自然布局 21%、环形布局 19.3%；真实浏览器截图确认紧凑效果。复杂关系仍可能交叉或文字相互遮挡。
- 有头像角色姓名在下方居中，PNG 中实际头像像素和姓名位置检查通过；导出保持手动坐标、平移及缩放。
- 原生全屏状态、进入提示、约 3 秒消失、再次进入重新显示、Esc 清除提示均通过；全屏不残留普通通知。
- 导出路径字体 14px、询问文字 11px，文件夹图标按钮 28px；黄金夜主题浏览器截图目视检查通过。
- 设置窗口在 818px 和 620px 高度下，滚动到导出选项前后“完成”按钮坐标完全相同，始终处于窗口可视范围；点击正常关闭。
- 最终桌面包通过 PNG/JSON 独立目录、同名文件自动编号、询问模式取消，以及 120 张关系网落盘恢复回归。记录：`test-results/v213-final-delivery/result.json`。后台测试关闭渲染节流，截图目视核对使用真实浏览器。
- 构建归档的版本、Electron 入口、预加载脚本和全部前端资源与当前代码匹配，不含 data、test-results 或 Git 目录。
- D 盘安装包与发布附件均为 123749109 字节，SHA-256：`c5c252309edffbcf36cb605d4c107b2a7796180a9c0cbc67e30c04c57f1f9123`。TXT 使用 UTF-8 BOM 与 Windows 换行。

## 2.1.1 历史验证记录

验证日期：2026-09-22，Windows x64，全部使用独立合成书架。

- 12 个测试文件、38 项自动测试通过；TypeScript、生产构建及 Electron 入口语法检查通过。
- 浏览器完整操作回归通过：手动排列后切换多个阵营并恢复全部，所有节点坐标完全一致；新增第 13 个角色不移动原有 12 个角色。
- 5 档时节点与关系文字均为 27px、普通关系线宽 2.34；自动测试覆盖全部 5 档和选中/关联高亮状态。
- 固定档案后切换选中角色，图上及左侧选中状态更新、档案保持不变；取消固定后显示当前选择。
- 复制关系网预填“原名-副本”，修改名称后创建；13 个角色及所有手动位置复制并在刷新后保持。
- 浏览器全屏实测通过，菜单和画布工具隐藏，Esc 恢复；帮助版权与按钮底部差小于 0.001px，截图目视检查通过。
- 浏览器导出设置通过实际 UI 操作，使用隔离的源私有文件系统（OPFS）替代系统目录选择器：独立 PNG/JSON 目录、开关持久化、实际 PNG（546304 字节）与 JSON（6310 字节）写入、JSON 不含坐标、开启询问后传入正确默认目录。此测试不代表手工操作 Windows 文件夹选择器。
- Electron 最终应用实测真实系统目录导出：PNG 含头像，导出前后节点位置、平移及缩放不变；JSON 和 PNG 分目录；自动导出不触发保存弹窗，同名 JSON 自动编号；询问模式使用指定默认目录并可取消。
- Electron 原生窗口 `isFullScreen()` 进入/退出均正确，纯图谱样式与原生状态同步；120 张关系网真实落盘与恢复通过。
- 新增设置界面截图检查后修正复选框同行左对齐，并通过浏览器计算样式复核。
- 最终包的一次后台 PNG 导出测试超过原 10 秒等待窗口；将测试等待延长到 20 秒并增加失败界面采集后，原安装包完整回归通过，没有绕过 PNG 内容或位置断言。
- 最终交付包测试记录：`test-results/v211-desktop-delivery/result.json`。打包内 Electron 入口、预加载脚本、导出模块及页面与当前源码匹配，未包含 data、test-results 或 Git 目录。
- D 盘 Windows x64 NSIS 安装包 123748456 字节，SHA-256：`0e6dddc03f235438250f46dab160f24dabbfc9167c9e1e9d744def5b26fa7bf8`；TXT 使用 UTF-8 BOM 与 Windows 换行。安装包与用于发布的英文名附件校验一致。

## 2.1.0 历史验证记录

验证日期：2026-09-21，Windows x64，全部使用独立合成书架。

- 10 个测试文件、33 项自动测试通过；TypeScript 检查、生产构建和 Windows 安装包构建通过。
- 实际浏览器上传 300×100 的红/蓝/绿测试图，自动取中间 100×100 蓝色正方形，检查左右边缘像素均为蓝色；姓名显示于圆形头像右侧。最终角色编辑窗口截图确认布局正常。
- 头像大小 1–5 档实测直径分别为 76、91.2、106.4、121.6、136.8；保存和重载后档位保持，与排列稀疏程度独立。
- 同一对人物的两条关系分别得到不同弧线控制点；另一对只有一条关系时保持直线。自动测试覆盖删掉多余关系后恢复直线。
- 表单修改未提交时显示“编辑中，尚未提交”，取消后原数据不变；拖动后自动保存。模拟保存接口返回 503 后显示“保存失败”，恢复接口并重新保存后显示成功。
- 连续拖动、刷新、切换两张关系网再返回，坐标与保存值完全一致；修复恢复位置对象被图形引擎共享、导致后续拖动无法正确识别的情况。两张图的位置独立保存。
- 实际导出单图与完整书架 JSON：保留头像及大小设置，不含本机坐标；导入默认忽略本机位置。对应自动测试覆盖无效坐标过滤与数据隔离。
- 带头像 PNG 实际输出 1484×982，像素检查包含 56,715 个蓝色头像像素；图片目视检查通过，导出前后节点坐标、平移和缩放完全一致。
- 角色档案滚动条隐藏，内容仍可滚动，底部操作保持固定；重命名铅笔下移 2px。
- 最终 Electron 包在隔离目录运行通过：实际 PNG 文件含头像、保存手动位置、5 档稀疏设置，以及保存恢复 120 张关系网。记录见 `test-results/v210-desktop/result.json`。
- D 盘安装包 `织灵-安装程序-2.1.0.exe` 为 123744742 字节；与用于 GitHub 发布的 `Zhiling-Setup-2.1.0.exe` 校验一致，SHA-256：`56BB1AD8690AF7F07BF1275FCF01E9611C5D95759CA4B675F596495C2CF9FAA9`。

## 2.0.6 历史验证记录

验证日期：2026-09-21，Windows x64。全部交互验证使用隔离的合成书架。

- 8 个测试文件、28 项自动测试通过；TypeScript 检查、生产构建、NSIS 安装包和便携程序生成成功。
- PNG 导出使用独立的节点坐标与数据快照，不重新布局。浏览器实际拖动节点后导出，节点坐标、平移与缩放前后完全一致；实际输出图片已目视检查，长姓名在圆圈内换行。桌面版另实测输出 1120×560 PNG，导出前后画布同样保持不变。
- 最终桌面应用使用独立测试目录启动，验证旧版双向标签迁移、实际 PNG 文件保存，以及 120 张关系网的保存与恢复。记录见 `test-results/revision8-desktop/result.json`。
- 关系线使用 straight 样式；25 条合成关系均没有曲线控制点或折线点。同一对人物的多条关系文字横排分行，避免竖直线上的文字重叠。自动布局通过移动无关节点减少线条被遮挡；高密度关系仍可能交叉。
- 真实浏览器验证：一个角色可同时属于多个阵营，筛选两个不同阵营均能找到该角色；新建、保存、刷新和再次编辑后归属保持。切换阵营和恢复全部阵营时，可见节点完整位于画布范围内。
- 新增关系的两端列表按拼音排序；已验证“安然、白露、海伦娜·莫兰、李四、张三”的顺序。新增人物的“所选角色 → 新角色”实际保存为正确方向。
- 颜色选择器包含明度/饱和度色块、色相条和手动输入，鼠标与键盘操作有效，拒绝越界 RGB。连续添加 8 种自定义颜色后保留后 7 种，刷新后保持；6 个预设、7 个历史色与加号同排等宽等距。关闭调色板不会误关角色表单。
- 档案长列表滚动前后，底部聚焦和删除按钮的位置不变。关系说明的“〔本人〕”置于对方姓名之后，保留箭头含义；角色 ID 注释已移除。
- 帮助版权文字左对齐；黄金夜搜索框文字为浅色、角色列表滚动条为低亮度深灰。已检查固定侧栏、调色板与颜色行截图。
- D 盘交付 2.0.6 安装程序、简明 TXT 说明与从源码打包的 Markdown 教程；安装包复制后核对 SHA-256。
  - 安装包：`D:\织灵-安装程序-2.0.6.exe`，123742300 字节。
  - SHA-256：`8A30CF0CB06795A7EE68B579D88A9CC241756DE457D8340BE9BB8D461187A8D7`。

## 2.0.5 历史验证记录

验证日期：2026-09-21，Windows x64。

- 21 项自动测试、TypeScript 检查和生产构建通过；NSIS 安装包与便携程序生成成功。
- 使用隔离书架在真实浏览器中验证：本人标记无图标、无边框、无背景，仅显示主题色“〔本人〕”；入向、出向、自身关系及真实姓名为“本人”的角色可正确区分。
- 主题顺序为黑白灰、黄金夜、浅草、碧蓝、桃粉；桃粉与碧蓝的背景、按钮和线条均降低饱和度，截图检查通过，刷新后所选主题保持。
- 帮助窗口底部靠右显示“© 2026 Symplatt 版权所有”，位于内容最后一行，未超出弹窗。
- 最终打包的桌面程序在独立测试数据目录中启动、实际导出 PNG、保存恢复 120 张关系网均通过，记录在 `test-results/revision6-desktop/result.json`。
- 安装使用说明以用户提供的 2.0.4 文档为底稿精简，更新至 2.0.5；发布 TXT 使用 UTF-8 BOM 和 Windows 换行。

## 2.0.4 历史验证记录

验证日期：2026-09-21，Windows x64。

- 21 项自动测试与生产构建通过，最终 NSIS 安装包及免安装程序生成成功。
- 角色档案的编辑按钮位于右上角，与关闭按钮同排；点击可打开正确角色的编辑表单。
- 本人徽标显示为“〔本人〕”并带人物图标及说明；已用真实姓名为“本人”的合成角色验证区分，并覆盖入向、出向和自身关系。
- 问号帮助窗口显示“作者：Symplatt”。
- 6 个预设颜色始终存在；自定义颜色接受六位十六进制、RGB 函数和三通道数值，拒绝非法或越界输入。
- 连续添加 8 种不同颜色后只保留后 7 种，重复颜色不占新位置、不重排队列；角色保存和页面重载后历史保持。
- 自定义边框颜色在选中状态仍按人物实际颜色绘制，外环标识选中状态。
- 最终 Electron 应用隔离运行、PNG 实际导出及 120 张关系网保存恢复通过，记录在 `test-results/revision5-desktop/result.json`。
- D 盘交付新版安装包及带 UTF-8 BOM、Windows 换行的 TXT 安装使用说明；安装包复制前后 SHA-256 校验一致。

## 2.0.3 历史验证记录

验证日期：2026-09-21，Windows x64。

- 17 项自动测试、TypeScript 检查及生产构建通过；生成内置 Electron 运行时的一键 NSIS 安装包和免安装程序。
- 浏览器检查五种主题：黑白灰、桃粉、碧蓝纸面分别调整为 `#e4e5e7`、`#eddee3`、`#dee8ef`；浅草与金夜底色保持原值。
- 选中节点采用细双环和低浓度染色，关联节点无外部光圈，连线不再加粗文字；五种主题的选中样式均生效。
- 新建/编辑双向关系只有一个关系名称输入栏；新增人物的初始关系也没有模式选择。
- 旧“母亲/女儿”成对关系合并后可直接编辑成“母女”，保存与重载不重复拼接、不丢失关系描述。
- 浏览器验证新增人物及初始双向关系成功；隔离的 Electron 实际导出 PNG、旧数据迁移、120 张关系网保存恢复通过，记录在 `test-results/revision4-desktop/result.json`。
- 安装包已复制到 `D:\织灵-安装程序-2.0.3.exe`，共 123738176 字节；复制前后 SHA-256 一致：`C424A297EC8B134B16B05D489F30A0756FDF4968C24A1E82EA8A0B962C364F11`。

## 2.0.2 历史验证记录

验证日期：2026-09-21，Windows x64。

- TypeScript 检查（含未使用变量检查）、生产构建和 Windows 便携版打包通过；16 项自动测试通过。
- 浏览器真实滚轮事件：单次 deltaY = -120，缩放从 98% 增至 129%。节点和导出使用同一套更疏朗的布局参数，关系字号为 15px。
- 真实点击人物圆圈：选中角色强高亮，全部入边、出边、多重关系和一度邻居弱高亮。自动测试同时覆盖自环、切换角色、选择关系、取消选择及聚焦模式。
- 原 graph-header 和 graph-toolbar 不再存在；画布上沿直接衔接顶部导航，应用宽度覆盖视口。导入使用向下图标、导出使用向上图标。
- 角色新增、空格标签、撤销重做、成对双向关系、重命名、五种主题与保存重载均通过浏览器验证。
- 导入旧 JSON 后移除网络简介，保留关系描述、人物小传和扩展字段；新建与重命名弹窗只需填写名称。
- 阵营筛选下整图 PNG 导出成功：7 角色合成图输出 2150×956、148399 字节。画布过滤与选中高亮不进入导出结果。
- 实际通过顶部导入按钮导入 120 张合成关系网，在书架搜索并切换到第 120 张，刷新后保持；新建关系网成功。
- 900×720 窄窗口下导航未溢出；新增角色弹窗为 560×367.4，水平和垂直中心均与视口中心重合。
- 更新后的 Electron 包实际打开并导出 PNG：2 角色图输出 160×704、23759 字节；120 张关系网保存恢复通过。记录在 `test-results/revision3-desktop/result.json`。
- `App.vue` 为 38 行，最大 Vue 组件为约 230 行；图谱样式、布局、选择和图片导出已独立为模块。
- 正式本地服务已返回新版构建资源，桌面快捷方式保持兼容。测试数据仅写入隔离测试目录。

## 2.0 历史验证记录

验证日期：2026-09-17，Windows x64。

- 生产构建及 TypeScript 检查通过；14 项自动测试通过。
- 合成数据验证：120 张关系网保存到 JSON、重新加载、选中图恢复、主题恢复；过期写入被拒绝；损坏主文件可从备份恢复。
- 浏览器实际操作：新增角色弹窗水平/垂直中心与真实视口中心差小于 3 像素，边缘在可视区内；无头像控件；“侠客 仙子 金丹后期”生成三个标签。
- 悬停显示所有其他人物，包括“暂无直接关系”；固定后移开保留；另一个图钉替换；窗口始终为一个；关闭不恢复旧窗口；再次点击同一图钉取消固定。
- “师父/徒弟”分别填写和“师徒”共同关系均可创建、编辑与显示，反向视角的关系正确。
- 黑白灰、浅草、桃粉、碧蓝、金夜的背景色各不相同，主题刷新后保留。
- 筛选到一个角色后导出完整图谱 PNG，校验类型、PNG 文件头及图片尺寸成功，当前筛选不受影响。合成的 4 角色图输出 1114×444 PNG。
- 实际导入 120 张图到浏览器书架后，可搜索切换到第 120 张，刷新保持；新建独立图不覆盖旧图。
- 最终打包的 Electron 版本：实际调用图片导出按钮，PNG 写入磁盘，2 角色双向图输出 160×574，22,010 字节；120 张图保存恢复通过。结果见 `test-results/zhiling-desktop/result.json`。
- 桌面 `织灵.lnk` 指向原生浏览器启动器。启动该快捷方式后，本地服务 `/api/health` 返回 `app: zhiling, version: 2`，默认浏览器打开应用。
- 正式浏览器书架与隔离测试端口分开，交互测试只使用合成数据，不导出用户故事到测试目录。

原有节点防重叠算法和 500 节点测试继续保留。高密度边及其标签仍可能交叉，不承诺任意复杂图的边完全无交叉。
