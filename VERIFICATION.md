# 交付验证记录

验证日期：2026-09-17。环境：Windows 11 x64、Node.js 24.12、Electron 44.4.1。

## 已通过

- Vue / TypeScript 类型检查及 Vite 生产构建。
- 9 项自动测试：最小 JSON 兼容、重复 ID、悬空关系、删除级联、扩展字段保留、头像格式、500 角色数据、500 节点重合时的间距修正、已有非重叠布局保留。
- 实际界面：新增角色、编辑姓名与小传、双向关系、新增与编辑关系、刷新后恢复、角色搜索、删除级联、撤销与重做、重复 ID 导入拒绝、导入预览、同对人物多关系、新建故事、阵营筛选、两种布局。
- 500 角色 / 499 关系实际界面压力测试：导入与布局约 1,013 ms，节点碰撞计数 0，非法坐标 0。按阵营筛选后显示 100 个节点。此时间只代表本机本次测试。
- 头像上传后同时出现在详情及 Cytoscape 节点中；移除头像恢复姓名显示。
- 浏览器导出 JSON 内容：12 角色 / 18 关系、标题与原始示例一致。Tabbit 下载事件等待未返回，改为读取实际导出 Blob 校验；桌面原生导出另有落盘验证。
- 桌面集成：Vue 实际页面渲染、隔离的 preload 接口、渲染器无法访问 Node require、JSON 保存与读取、按顺序原子替换、上一份备份、本地相对头像解析、越界图片回退、原生导出落盘、取消导入。
- 关闭后重新启动 Electron，成功恢复已保存的故事名称、2 个角色及 1 条双向关系。

最终免安装 exe 的启动与正常退出结果见 `test-results/portable-result.json`。界面预览见 `test-results/desktop-app.png`。

## 已知边界

- 算法保证自动排列后的节点/节点文字留出间距；密集边交叉及边标签遮挡仍可能存在。
- 大量头像会显著增大 JSON 和内存占用。头像限制 5 MB、整个 JSON 限制 64 MB。
- 当前维护单个活动故事。通过导入/导出切换多个故事；不同故事不会自动合并。
- 不保存手动位置、缩放和显示偏好；下次打开重新布局。撤销记录只在当前会话内有效。
- 便携 exe 未附付费数字签名；可能显示 Windows 未知发布者提示。
