# 织灵 2.0.5 验证记录

验证日期：2026-09-21，Windows x64。

- 21 项自动测试、TypeScript 检查和生产构建通过；NSIS 安装包与便携程序生成成功。
- 使用隔离书架在真实浏览器中验证：本人标记无图标、无边框、无背景，仅显示主题色“〔本人〕”；入向、出向、自身关系及真实姓名为“本人”的角色可正确区分。
- 主题顺序为黑白灰、黄金夜、浅草、碧蓝、桃粉；桃粉与碧蓝的背景、按钮和线条均降低饱和度，截图检查通过，刷新后所选主题保持。
- 帮助窗口底部靠右显示“© 2026 Symplatt 版权所有”，位于内容最后一行，未超出弹窗。
- 最终打包的桌面程序在独立测试数据目录中启动、实际导出 PNG、保存恢复 120 张关系网均通过，记录在 `test-results/revision6-desktop/result.json`。
- 安装使用说明以用户提供的 2.0.4 文档为底稿精简，更新至 2.0.5；发布 TXT 使用 UTF-8 BOM 和 Windows 换行。

## 2.0.4 历史验证记录

验证日期：2026-09-21，Windows x64。

- 21 项自动测试与生产构建通过，最终 NSIS 安装包及免安装程序生成成功。
- 角色档案的编辑按钮位于右上角，与关闭按钮同排；点击可打开正确角色的编辑表单。
- 本人徽标显示为“〔本人〕”并带人物图标及说明；已用真实姓名为“本人”的合成角色验证区分，并覆盖入向、出向和自身关系。
- 问号帮助窗口显示“作者：Symplatt”。
- 6 个预设颜色始终存在；自定义颜色接受六位十六进制、RGB 函数和三通道数值，拒绝非法或越界输入。
- 连续添加 8 种不同颜色后只保留后 7 种，重复颜色不占新位置、不重排队列；角色保存和页面重载后历史保持。
- 自定义边框颜色在选中状态仍按人物实际颜色绘制，外环标识选中状态。
- 最终 Electron 应用隔离运行、PNG 实际导出及 120 张关系网保存恢复通过，记录在 `test-results/revision5-desktop/result.json`。
- D 盘交付新版安装包及带 UTF-8 BOM、Windows 换行的 TXT 安装使用说明；安装包复制前后 SHA-256 校验一致。

## 2.0.3 历史验证记录

验证日期：2026-09-21，Windows x64。

- 17 项自动测试、TypeScript 检查及生产构建通过；生成内置 Electron 运行时的一键 NSIS 安装包和免安装程序。
- 浏览器检查五种主题：黑白灰、桃粉、碧蓝纸面分别调整为 `#e4e5e7`、`#eddee3`、`#dee8ef`；浅草与金夜底色保持原值。
- 选中节点采用细双环和低浓度染色，关联节点无外部光圈，连线不再加粗文字；五种主题的选中样式均生效。
- 新建/编辑双向关系只有一个关系名称输入栏；新增人物的初始关系也没有模式选择。
- 旧“母亲/女儿”成对关系合并后可直接编辑成“母女”，保存与重载不重复拼接、不丢失关系描述。
- 浏览器验证新增人物及初始双向关系成功；隔离的 Electron 实际导出 PNG、旧数据迁移、120 张关系网保存恢复通过，记录在 `test-results/revision4-desktop/result.json`。
- 安装包已复制到 `D:\织灵-安装程序-2.0.3.exe`，共 123738176 字节；复制前后 SHA-256 一致：`C424A297EC8B134B16B05D489F30A0756FDF4968C24A1E82EA8A0B962C364F11`。

## 2.0.2 历史验证记录

验证日期：2026-09-21，Windows x64。

- TypeScript 检查（含未使用变量检查）、生产构建和 Windows 便携版打包通过；16 项自动测试通过。
- 浏览器真实滚轮事件：单次 deltaY = -120，缩放从 98% 增至 129%。节点和导出使用同一套更疏朗的布局参数，关系字号为 15px。
- 真实点击人物圆圈：选中角色强高亮，全部入边、出边、多重关系和一度邻居弱高亮。自动测试同时覆盖自环、切换角色、选择关系、取消选择及聚焦模式。
- 原 graph-header 和 graph-toolbar 不再存在；画布上沿直接衔接顶部导航，应用宽度覆盖视口。导入使用向下图标、导出使用向上图标。
- 角色新增、空格标签、撤销重做、成对双向关系、重命名、五种主题与保存重载均通过浏览器验证。
- 导入旧 JSON 后移除网络简介，保留关系描述、人物小传和扩展字段；新建与重命名弹窗只需填写名称。
- 阵营筛选下整图 PNG 导出成功：7 角色合成图输出 2150×956、148399 字节。画布过滤与选中高亮不进入导出结果。
- 实际通过顶部导入按钮导入 120 张合成关系网，在书架搜索并切换到第 120 张，刷新后保持；新建关系网成功。
- 900×720 窄窗口下导航未溢出；新增角色弹窗为 560×367.4，水平和垂直中心均与视口中心重合。
- 更新后的 Electron 包实际打开并导出 PNG：2 角色图输出 160×704、23759 字节；120 张关系网保存恢复通过。记录在 `test-results/revision3-desktop/result.json`。
- `App.vue` 为 38 行，最大 Vue 组件为约 230 行；图谱样式、布局、选择和图片导出已独立为模块。
- 正式本地服务已返回新版构建资源，桌面快捷方式保持兼容。测试数据仅写入隔离测试目录。

## 2.0 历史验证记录

验证日期：2026-09-17，Windows x64。

- 生产构建及 TypeScript 检查通过；14 项自动测试通过。
- 合成数据验证：120 张关系网保存到 JSON、重新加载、选中图恢复、主题恢复；过期写入被拒绝；损坏主文件可从备份恢复。
- 浏览器实际操作：新增角色弹窗水平/垂直中心与真实视口中心差小于 3 像素，边缘在可视区内；无头像控件；“侠客 仙子 金丹后期”生成三个标签。
- 悬停显示所有其他人物，包括“暂无直接关系”；固定后移开保留；另一个图钉替换；窗口始终为一个；关闭不恢复旧窗口；再次点击同一图钉取消固定。
- “师父/徒弟”分别填写和“师徒”共同关系均可创建、编辑与显示，反向视角的关系正确。
- 黑白灰、浅草、桃粉、碧蓝、金夜的背景色各不相同，主题刷新后保留。
- 筛选到一个角色后导出完整图谱 PNG，校验类型、PNG 文件头及图片尺寸成功，当前筛选不受影响。合成的 4 角色图输出 1114×444 PNG。
- 实际导入 120 张图到浏览器书架后，可搜索切换到第 120 张，刷新保持；新建独立图不覆盖旧图。
- 最终打包的 Electron 版本：实际调用图片导出按钮，PNG 写入磁盘，2 角色双向图输出 160×574，22,010 字节；120 张图保存恢复通过。结果见 `test-results/zhiling-desktop/result.json`。
- 桌面 `织灵.lnk` 指向原生浏览器启动器。启动该快捷方式后，本地服务 `/api/health` 返回 `app: zhiling, version: 2`，默认浏览器打开应用。
- 正式浏览器书架与隔离测试端口分开，交互测试只使用合成数据，不导出用户故事到测试目录。

原有节点防重叠算法和 500 节点测试继续保留。高密度边及其标签仍可能交叉，不承诺任意复杂图的边完全无交叉。
